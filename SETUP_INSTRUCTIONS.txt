================================================================================
    SUPABASE DATABASE SETUP - STEP BY STEP
================================================================================

📌 STEP 1: CREATE SUPABASE PROJECT
──────────────────────────────────────────────────────────────────────────────
1. Go to: https://supabase.com
2. Click: "New Project"
3. Enter:
   - Project Name: scholarship-gateway
   - Database Password: [YOUR_STRONG_PASSWORD]
   - Region: [CLOSEST_TO_YOU]
4. Click: "Create new project"
5. Wait: 2-3 minutes for setup

================================================================================

📌 STEP 2: RUN DATABASE SCHEMA
──────────────────────────────────────────────────────────────────────────────
1. In Supabase Dashboard:
   - Click: "SQL Editor" (left sidebar)
   - Click: "New Query"
   
2. Open this file: backend/supabase-schema.sql
   
3. Copy ALL the SQL code from that file

4. Paste into Supabase SQL Editor

5. Click: "Run" button

6. You should see: "Success. No rows returned"

✅ DATABASE TABLES CREATED!

================================================================================

📌 STEP 3: GET YOUR SUPABASE KEYS
──────────────────────────────────────────────────────────────────────────────
1. In Supabase Dashboard:
   - Click: Settings (⚙️ icon at bottom)
   - Click: "API" (left sidebar)

2. Copy these 3 things:

   a) Project URL:
      Example: https://abcdefghijk.supabase.co
      
   b) anon public key:
      Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3...
      
   c) service_role key:
      Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3...

⚠️  IMPORTANT: service_role key is SECRET! Don't share it!

================================================================================

📌 STEP 4: CREATE STORAGE BUCKET
──────────────────────────────────────────────────────────────────────────────
1. In Supabase Dashboard:
   - Click: "Storage" (left sidebar)
   - Click: "Create a new bucket"

2. Enter:
   - Name: images
   - Public bucket: ✅ CHECK THIS
   
3. Click: "Create bucket"

✅ STORAGE READY FOR IMAGE UPLOADS!

================================================================================

📌 STEP 5: CREATE .env FILE IN BACKEND
──────────────────────────────────────────────────────────────────────────────
1. In your backend folder, create a file named: .env

2. Copy this template and REPLACE with your actual keys:

─────────────────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=development

SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY_HERE

SUPABASE_BUCKET=images
CORS_ORIGINS=http://localhost:3000
─────────────────────────────────────────────────────────────────────────────

3. Save the file

⚠️  NO QUOTES around values!
⚠️  NO SPACES before or after =
⚠️  Keys should be complete (very long strings)

================================================================================

📌 STEP 6: START BACKEND SERVER
──────────────────────────────────────────────────────────────────────────────
Open terminal/PowerShell:

cd backend
npm install
npm run dev

You should see:
🚀 Server is running on port 5000
📡 Environment: development
✅ Bucket 'images' already exists

================================================================================

📌 STEP 7: TEST CONNECTION
──────────────────────────────────────────────────────────────────────────────
Open browser and go to:

http://localhost:5000/api/health

You should see:
{
  "status": "ok",
  "message": "Daily Better Journey API is running",
  "timestamp": "..."
}

✅ BACKEND CONNECTED TO SUPABASE!

================================================================================

📌 STEP 8: START FRONTEND
──────────────────────────────────────────────────────────────────────────────
Open NEW terminal/PowerShell:

cd frontend
npm install
npm run dev

Open browser:
http://localhost:3000

✅ SITE IS RUNNING!

================================================================================

🎉 SETUP COMPLETE!
──────────────────────────────────────────────────────────────────────────────

Your site is now connected to your NEW Supabase database!

Next steps:
1. Go to: http://localhost:3000/admin
2. Create categories (e.g., "Undergraduate Scholarships")
3. Write scholarship articles
4. Publish and test!

================================================================================

🔧 TROUBLESHOOTING
──────────────────────────────────────────────────────────────────────────────

Problem: "Cannot connect to database"
Solution: Check SUPABASE_URL in .env file

Problem: "Invalid API key"
Solution: Re-copy keys from Supabase (no extra spaces!)

Problem: "CORS error"
Solution: Check CORS_ORIGINS=http://localhost:3000 in .env

Problem: "Bucket not found"
Solution: Create 'images' bucket in Supabase Storage

================================================================================

📁 YOUR .env FILE LOCATION:
backend/.env  ← Create this file with your keys!

📄 SCHEMA FILE LOCATION:
backend/supabase-schema.sql  ← Copy SQL from here!

================================================================================

